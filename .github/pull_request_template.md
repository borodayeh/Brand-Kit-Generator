# Pull request

## What does this change? / این تغییر چه می‌کند؟

<!-- A short description, in English or Persian. -->

## Checklist

- [ ] `ruff check .` passes
- [ ] `mypy` passes
- [ ] `pytest` passes
- [ ] `python scripts/validate_templates.py` passes
- [ ] New user-facing strings added to `MESSAGES` in **both** `en` and `fa`
- [ ] Persian output stays wrapped in `<div dir="rtl">`
- [ ] `CHANGELOG.md` updated
